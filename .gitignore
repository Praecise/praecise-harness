node_modules/
dist/
.praecise/
.env
.env.local
*.log
.DS_Store

# packing artefacts — never committed, and never shipped
*.tgz
.tmp-ts-*

# Local tool state (tenzro-code writes a ledger here). Never part of the package.
.tenzro-code/
